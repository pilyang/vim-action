# Normal mode exit 키 허용목록 — "선택한 조합에서만 Insert 전환"

- **생성일**: 2026-08-18
- **갱신일**: 2026-08-18

## 목표

`Exit Normal mode on ⌘/⌥ shortcuts` 설정을 전체 on/off 이분법에서, **기본은 전환 안 함 + 사용자가 등록한 키 조합에서만 Normal → Insert 자동 전환**하는 형태로 고도화한다. 등록은 텍스트 입력이 아니라 **원하는 키 조합을 직접 눌러서** 추가하는 UI로 한다.

## 완료된 것

- 없음 (미착수 — 예정 기록용 플랜)

## 남은 것

- [ ] 현재 토글(`SettingsView.swift`의 `isNormalModeEscapeEnabled`)과 엔진 쪽 escape 판정 지점 파악
- [ ] 설정 모델 설계: 기존 on/off와 새 허용목록의 관계(기존 사용자 설정 이관 포함) 결정
- [ ] 설정 UI: "Normal mode exit 키 추가하기" → 키 조합 직접 입력(캡처) → 등록 / 목록에서 삭제
- [ ] 엔진·실행 경로 반영 및 테스트

## 진행 중 컨텍스트

미착수. 아직 아무 설계도 확정되지 않았고, 구현 방식은 착수 시점에 정한다.

기록 시점 사실관계만: 현재는 `SettingsView.swift:118`의 단일 토글 하나뿐이고, 켜면 ⌘/⌥ 단축키 뒤 Insert로 복귀, 끄면 Normal 유지 + 단축키는 앱으로 통과한다.

## 관련 링크

- 없음
