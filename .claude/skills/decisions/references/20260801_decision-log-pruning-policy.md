# 결정 로그 정리 정책 — 소진된 폐기 문서는 삭제, 인덱스는 압축 유지

<!-- 파일명 규칙: yyyymmdd_<kebab-case-title>.md — 날짜는 결정일. 이 문서는 결정의 불변 스냅샷이며, 기록 후 수정하지 않습니다 (Superseded 마킹 1줄 제외). -->
<!-- 참고: 코드 아키텍처 결정이 아닌 결정 로그 자체의 운영 정책이라 SKILL.md 인덱스에는 등재하지 않는 standalone 기록입니다 (20260712_plans-skill-for-short-term-memory.md와 같은 방식). -->

- **결정일**: 2026-08-01

## 결정

1. **완전 폐기 문서는 삭제할 수 있다.** supersede로 인덱스에서 제거된 문서 중 히스토리 가치까지 소진된 것(단명한 과도기 규칙, 이행이 끝난 스케줄링 결정)은 `references/`에서 파일도 삭제한다 — git 히스토리가 아카이브다. 실측 기록·위험 분석 등 재참조 가치가 남았거나 애매하면 파일은 남긴다. 첫 적용으로 5건 삭제(main-runloop-tap-attachment, replace-swallow-transitional-rule, release-block-gate-moves-to-m3, count-clamp-retained-at-9999, scroll-page-key-convergence).
2. **이행 완료된 스케줄링 결정은 인덱스에서 내린다.** supersede가 아니어도 결정의 효력이 소진된 문서(예: "래치를 M3로 승격" — 승격 완료, "게이트 판정 항목으로 상정" — 판정 완료)는 상단에 `> 이행 완료 — ...` 마킹 후 인덱스에서 제외한다. 첫 적용 2건(count-burst-abort-latch-promotion, cmd-z-ansi-layout-escalation) — 실측·위험 분석 기록이 남아 파일은 유지.
3. **인덱스 요약은 한 줄로 압축, 도메인별 섹션 구조.** 상세 근거는 문서 몫이고 인덱스는 탐색용이다.

## 배경·근거 (왜)

결정 108건 시점에 SKILL.md가 약 4,900단어까지 비대해졌다 — 개별 문서는 조회 시에만 읽히므로, 매 로드마다 주입되는 비용은 전부 인덱스에 있었다(행당 요약이 사실상 미니 문서). 또 supersede 체인이 끝까지 종결된 문서(예: 수명 1일이었던 클램프 유지 결정)는 "그때 왜 그랬는지"까지 후속 문서가 요약하고 있어, references/에 남겨도 아무도 읽지 않으면서 디렉토리 탐색 소음만 늘렸다. plans 스킬의 "완료 플랜 아카이브 금지 — git이 복구 수단" 원칙과 같은 근거다.

## 검토한 대안

- **references/에 전부 보존 (기존 원칙)**: 폐기 문서의 유일한 소비처가 "히스토리 조사"인데, 그 용도는 git 히스토리로 동일하게 충족된다. 소진 문서가 쌓이는 비용만 남아 완화.
- **인덱스 행 자체를 대폭 삭제**: 수용 엣지·M2 배선 등 아직 유효한 제약 기록이 대량 소실된다 — 기각. 특히 수용 엣지 문서들은 M4 프로파일·M5 AX 설계의 입력이라 그 시점에 일괄 supersede하는 것이 올바른 정리 경로.
- **요약을 지금 수준으로 유지**: 행당 요약이 길수록 인덱스만 보고 결정을 "아는 것처럼" 다루게 되는 부작용도 있었다(문서를 안 읽음). 한 줄 요약 + 문서 읽기 유도가 원래 설계 의도에 가깝다.
