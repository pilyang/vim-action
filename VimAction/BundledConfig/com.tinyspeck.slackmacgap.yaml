# Slack — Return **sends the message** instead of inserting a line break.
# If o/O synthesized Return, a half-written message would be sent, so open_line is off.
# (Pressing o still enters Insert mode but no line is added — press Esc to go back.)
name: Slack
actions:
  open_line: disabled
